# name: CI

# on:
#   push:
#     branches: [main]
#   pull_request:

# permissions:
#   contents: read

# jobs:
#   checks:
#     strategy:
#       matrix:
#         os: [ubuntu-latest, macos-latest, windows-latest]
#     runs-on: ${{ matrix.os }}
#     steps:
#       - uses: actions/checkout@v6
#       - uses: prefix-dev/setup-pixi@v0.10.1
#         with:
#           pixi-version: v0.63.2
#           locked: true
#           cache: true
#       - run: pixi run test
#       - run: pixi run lint
#       - run: pixi run format-check
#       - run: pixi run typecheck

#   package:
#     needs: checks
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/checkout@v6
#       - uses: prefix-dev/setup-pixi@v0.10.1
#         with:
#           pixi-version: v0.63.2
#           locked: true
#           cache: true
#       - run: pixi run python -m build --wheel --no-isolation packages/runbook/runbook-core
#       - run: pixi run python -m build --wheel --no-isolation packages/runbook/runbook-data
#       - run: pixi run python -m build --wheel --no-isolation packages/runbook/runbook-platform
#       - run: pixi run python -m build --wheel --no-isolation packages/runbook/runbook-sdk
#       - run: pixi run python -m build --wheel --no-isolation packages/runbook/runbook-services

#   postgres:
#     needs: checks
#     runs-on: ubuntu-latest
#     services:
#       postgres:
#         image: postgres:16
#         env:
#           POSTGRES_PASSWORD: postgres
#           POSTGRES_DB: runbook
#         ports: [5432:5432]
#         options: >-
#           --health-cmd "pg_isready -U postgres -d runbook"
#           --health-interval 10s
#           --health-timeout 5s
#           --health-retries 5
#     steps:
#       - uses: actions/checkout@v6
#       - uses: prefix-dev/setup-pixi@v0.10.1
#         with:
#           pixi-version: v0.63.2
#           locked: true
#           cache: true
#       - run: pixi run runbook-services --database postgresql+psycopg://postgres:postgres@localhost:5432/runbook db upgrade
